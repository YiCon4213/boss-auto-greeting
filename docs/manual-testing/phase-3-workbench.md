# Phase 3 分析与审批工作台验证记录

本记录只验证 Phase 3 的模型契约、分析/话术/审批门禁和本地工作台。它不访问 BOSS、不执行发送、不自动登录，也不处理或绕过验证码、安全校验和访问控制。

## 自动化前置门禁

- [x] 81 项 pytest 通过，`agent_app` 覆盖率 92.42%。
- [x] Alembic 从空 SQLite 升级到 `0003 (head)`。
- [x] `python -m compileall agent_app` 通过。
- [x] `node --check agent_app/web/app.js` 与 `node --check zhipin-auto-greeting.user.js` 通过。
- [x] 分析关闭、目标分边界、个人分不淘汰、私密字段过滤、模型失败、事实追溯和敏感输出清除均有自动化测试。
- [x] 批准后强 ID、JD 指纹和人工终稿不可变；重复批准幂等；批准动作不会创建 `execute_delivery` 任务。
- [x] 工作台契约禁止 HTML 字符串注入、远程 CDN、API Key 和 Phase 4 发送入口。

## 本机工作台检查

- 执行时间：2026-07-22
- 目标：`http://127.0.0.1:8765/`
- 桌面结果：顶部批次区、左侧岗位队列、右侧详情区和底部人工审批门禁按双栏布局显示；未加载批次时批准按钮禁用。
- 窄屏结果：视口设置为 390×844；浏览器报告内容视口宽度与页面滚动宽度均为 375px，两个主网格折叠为单列，无横向溢出。
- 审批按钮：边界为 `top=767.2`、`bottom=809.2`，位于 844px 高视口内且完整可见。
- 安全渲染：动态数据路径只使用 `textContent` 和表单 `value`；页面脚本不读取应用令牌，工作台会话 cookie 为 HttpOnly、SameSite=Strict 且路径限制为 `/api`。
- 范围确认：没有访问 BOSS、没有输入账号或验证码、没有调用真实模型、没有创建或执行发送任务。
- 清理：本地预览服务已停止，临时预览数据库目录已删除。

## 结果

Phase 3 工作台退出门禁通过。真实模型兼容性和本人登录环境下的安全发送属于后续阶段；出现验证码、登录失效或安全校验时仍必须立即暂停，不得尝试自动处理。
